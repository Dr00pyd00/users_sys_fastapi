[project]
name = "users-project"
version = "0.1.0"
description = "A fastapi project for good Users system"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "bcrypt>=5.0.0",
    "fastapi[standard]>=0.141.1",
    "sqlalchemy>=2.0.54",
]
